sed -i 's/const playheadRatio = duration > 0 ? currentTime \/ duration : 0;/const totalDurationRender = duration > 0 ? duration : 225.782;\n    const visibleDuration = totalDurationRender \/ zoomLevel;\n    let viewStart = currentTime - visibleDuration \/ 2;\n    if (viewStart < 0) viewStart = 0;\n    if (viewStart + visibleDuration > totalDurationRender) viewStart = Math.max(0, totalDurationRender - visibleDuration);\n\n    const playheadX = duration > 0 ? ((currentTime - viewStart) \/ visibleDuration) * width : 0;/' src/components/WaveformHero.tsx
